-- ============================================================
-- Migration 2026-07-27 — ชื่อเล่นแอดมิน (nickname)
-- รันใน Supabase: Dashboard → SQL Editor → วางทั้งไฟล์ → Run  (ปลอดภัย รันซ้ำได้)
--
-- ทำไมต้องอยู่ที่ admin_settings:
--   ตาราง `admins` ถูก sync เขียนทับทั้งตารางทุกชั่วโมง — ชื่อเล่นที่พิมพ์เองจะหายทุกรอบ
--   ส่วน `note` เป็นโน้ตอิสระที่ทีมใช้อยู่แล้ว (เช่น "ถนัดปิดการขาย LINE") ห้ามยัดรวม
--
-- ค่าว่าง ('') = ยังไม่เคยพิมพ์ทับ → โค้ดจะ "เดาอัตโนมัติ" จากคำแรกของชื่อ Pancake
--   ("หมีน้อย สีน้ำตาล" → "หมีน้อย", "Eiei Eiei" → "Eiei")
-- โค้ดฝั่งเว็บทำงานได้ปกติแม้ยังไม่รันไฟล์นี้ (query fallback ตัดคอลัมน์ nickname ออกให้เอง)
-- ============================================================

alter table admin_settings add column if not exists nickname text default '';
